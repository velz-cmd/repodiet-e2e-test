// RepoDiet production verification fixture — DISPOSABLE.
//
// Created only on a throwaway seed branch so the production cleanup engine has
// an unambiguous safe candidate to find. Nothing imports this module, and it
// exists on no other branch. It is not part of the repository main line and is
// not part of any paid job.

export function unusedVerificationHelper(a, b) {
  return a + b;
}

export const UNUSED_VERIFICATION_CONSTANT = 42;
